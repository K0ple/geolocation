{
  "name": "geolocation-nodejs",
  "version": "1.0.0",
  "description": "Test REST API in node.js to return location data of user",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "express": "^4.18.2"
  }
}
